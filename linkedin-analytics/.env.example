# LinkedIn Analytics Dashboard — Environment Variables
# Copy this file to .env and adjust values as needed.
# .env is gitignored and must never be committed.

# Port the dashboard listens on (host-side port for Docker)
APP_PORT=8050

# Directory where SQLite database and uploaded files are stored
# Inside Docker this maps to the container path via the bind mount.
DATA_DIR=/app/data

# Log level: debug, info, warning, error
LOG_LEVEL=info

# ---------------------------------------------------------------------------
# LinkedIn OAuth (optional)
# All three values must be set to enable OAuth features. If any are empty,
# the dashboard runs in manual-upload-only mode (no OAuth, no errors).
# ---------------------------------------------------------------------------

# LinkedIn developer app credentials.
# Register your app at https://developer.linkedin.com
# LINKEDIN_CLIENT_ID=
# LINKEDIN_CLIENT_SECRET=

# Fernet symmetric encryption key for storing tokens at rest.
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Keep a backup of this key. If lost, tokens become undecryptable and you must re-authorize.
# TOKEN_ENCRYPTION_KEY=

# LinkedIn API version header (format: YYYYMM). Update when LinkedIn releases breaking API changes.
# LINKEDIN_API_VERSION=202601

# OAuth callback URL. Must exactly match the redirect URI registered in the LinkedIn developer portal.
# For local development: http://localhost:8050/oauth/callback
# For production: use HTTPS (e.g. https://your-domain.example.com/oauth/callback)
# LinkedIn requires HTTPS for production redirect URIs.
# LINKEDIN_REDIRECT_URI=http://localhost:8050/oauth/callback
